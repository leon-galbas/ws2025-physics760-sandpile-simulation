\begin{table*}[!ht]
  \centering
  \input{../../figures/tables/scaling_exp_closed_nonconservative.tex}
  \caption{Scaling exponents of the probability density functions for the closed-boundary, nonconservative sandpile configurations (C, F, I). The exponents $\alpha$, $\tau$, and $\lambda$ are obtained from weighted linear least-squares fits on double-logarithmic PDF plots in the selected scaling window; uncertainties are taken from the fit error.}
  \label{tab:scaling_exp_closed_nonconservative}
\end{table*}

\begin{table*}[!ht]
  \centering
  \input{../../figures/tables/scaling_exp_closed_nonconservative_gamma.tex}
  \caption{Conditional scaling exponents for the closed-boundary, nonconservative sandpile configurations (C, F, I). The exponents $\gamma_1$, $\gamma_2$, and $\gamma_3$ (and their inverse fits) are obtained from weighted linear least-squares fits of conditional expectation values on double-logarithmic plots in the selected scaling window; uncertainties are taken from the fit error.}
  \label{tab:scaling_exp_closed_nonconservative_gamma}
\end{table*}

\begin{table*}[!ht]
  \centering
  \input{../../figures/tables/scaling_exp_open_conservative.tex}
  \caption{Scaling exponents of the probability density functions for the open-boundary, conservative sandpile configurations (B, E, H). The exponents $\alpha$, $\tau$, and $\lambda$ are obtained from weighted linear least-squares fits on double-logarithmic PDF plots in the selected scaling window; uncertainties are taken from the fit error.}
  \label{tab:scaling_exp_open_conservative}
\end{table*}

\begin{table*}[!ht]
  \centering
  \input{../../figures/tables/scaling_exp_open_conservative_gamma.tex}
  \caption{Conditional scaling exponents for the open-boundary, conservative sandpile configurations (B, E, H). The exponents $\gamma_1$, $\gamma_2$, and $\gamma_3$ (and their inverse fits) are obtained from weighted linear least-squares fits of conditional expectation values on double-logarithmic plots in the selected scaling window; uncertainties are taken from the fit error.}
  \label{tab:scaling_exp_open_conservative_gamma}
\end{table*}


